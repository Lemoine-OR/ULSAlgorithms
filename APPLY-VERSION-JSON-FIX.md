# ULSAlgorithms v0.14.0 - restore version.json

The current main branch is missing the repository-root `version.json`.

This package restores only that file with declared version `0.14.0`.

No workflow, solver, test, benchmark or documentation source is modified.

Apply at the repository root:
D:\Dev\UlsAlgorithm\ULSAlgorithms

Then verify:
1. `version.json` is visible next to `ULSAlgorithms.sln`.
2. Rebuild Release.
3. Run all tests.
4. Commit and push.
